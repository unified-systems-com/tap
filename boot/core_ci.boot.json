{
  "version": 1,
  "profile_kind": "dev_local",
  "description": "The core PR gate's profile (tap#366, stage 3 of the plugin-CI epic tap#363): core + the fixture plugins the core suite's coverage guards need + ONE flagship canary with its declared dependencies. NOT the full set — `test_all` keeps that role (it is the bill of materials the BOM lane boots); this profile is bounded by tap/tests/test_core_ci_profile.py, which refuses any plugin that is neither a fixture nor the canary's dependency closure, and which requires every pin here to equal test_all's for the same slug (one place a plugin version lives; scripts/release-plugin.sh bumps both). Seeds the canary's GRIFT so the core contract suite (tap_plugins/tests/test_plugin_contract.py) exercises a real plugin end to end; fires no collectors.",
  "install": {
    "plugins": [
      {
        "slug": "grid_fixtures",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-grid-fixtures",
          "rev": "v0.3.1",
          "commit": "7f08d0780d7468590507df2a5562fe730d76e8c8"
        },
        "note": "Fixture: the neutral grid_fixtures__* vocabulary the core suites build fixtures from (tap.plugin_testing.BASELINE_PLUGIN_SLUGS)."
      },
      {
        "slug": "gryphon_playground",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-gryphon-playground",
          "rev": "v0.5.0",
          "commit": "b52731675163b36d786d981b27acac4c95583a49"
        },
        "note": "Fixture: the Gridkin scenario corpus + harness that exercise the Gryphon query language; owns no types, depends_on grid_fixtures."
      },
      {
        "slug": "validation_sample",
        "enabled": true,
        "source": {
          "type": "editable",
          "path": "tap_plugins/tests/fixtures/validation_sample"
        },
        "note": "Fixture: the in-tree validate_plugin test target (tap_plugin.validation_sample); no GRIFT seed, no deps."
      },
      {
        "slug": "identity_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-identity-core",
          "rev": "v0.1.2",
          "commit": "63da037c77355369f91748127a74c7a17e00a7f9"
        },
        "note": "Canary dependency: github_core's manifest depends_on identity_core (the oidc_issuer convergence node + issuer helper); installed before the canary."
      },
      {
        "slug": "git_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/git-core-tap",
          "rev": "v0.1.0",
          "commit": "efa983a582eaef7d16e27daa50a68aeaef7c56de"
        },
        "note": "Canary dependency: github_core >=v0.10.0 mints git_core's repository/ref/commit ids through tap_plugin.git_core.identity and emits those neutral types. Not on PyPI, so it must be installed by an EARLIER entry than github_core or pre-boot dies on a PyPI lookup (tap#647, tap#675)."
      },
      {
        "slug": "compliance_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-compliance-core",
          "rev": "v0.3.0",
          "commit": "10871675ff1c67bfac0ca61d90ee12c507fd76f4"
        },
        "note": "Canary dependency: github_core >=v0.10.0 mints compliance_core__compliance_finding for every scanner alert and DETAILS_FINDING targets its type (github-core#89). Same pin as test_all; listed before github_core (tap#647, tap#675)."
      },
      {
        "slug": "github_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-github-core",
          "rev": "v0.4.0",
          "commit": "3356301e676094523943f2bbb055be501a6b42d6"
        },
        "note": "Canary: the flagship consumer plugin (the git-serious product's substrate). Its declared dependency closure is the only non-fixture set this profile may carry."
      }
    ]
  },
  "population": {
    "on_failure": "abort",
    "steps": [
      {
        "type": "seed-plugin",
        "plugin": "github_core",
        "enabled": true
      }
    ]
  }
}

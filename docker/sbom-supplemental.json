{
  "_description": "Out-of-band components of the tap-web image (req-cicd-sbom-3): everything that enters the image outside a package manager, declared here and merged into the generated SBOMs during the single derivation. Schema: scripts/sbom/supplemental.schema.json (loader-validated). Lives beside the Dockerfile so a pin bump and its SBOM entry change in the same diff. File sha256 is NOT declared here - it is computed from the scanned artifact at generation time (per-arch, derive-once).",
  "format": "tap-sbom-supplemental/1",
  "components": [
    {
      "_description": "Self-built OpenSSL FIPS provider (CMVP #4282) - compiled from pinned source in the ossl-builder stage; the most compliance-significant binary in the image and invisible to every scanner. First artifact of the crypto-BOM discipline (req-fips-crypto-bom). CPE added 2026-08-31 (req-cicd-sbom / tap#231): the purl is `pkg:generic`, which matches NO advisory feed, so the most compliance-significant binary in the image was invisible to every vulnerability scanner. CPE is the vocabulary NVD-backed matchers speak. VERIFIED against the live NVD API: this CPE returns 38 CVEs, of which 6 are other products (Mutt, OpenLDAP, mod_ssl) indexed under OpenSSL's CPE and most of the rest are in libcrypto/libssl code the FIPS provider does not contain — confirmed by inspecting the built fips.so (CMS/PKCS7/PKCS12/ASN.1/DANE/OCB symbols absent). So this field enables detection and REQUIRES triage: match on the CPE, then ask whether the vulnerable code is inside the FIPS module boundary before acting.",
      "name": "openssl-fips-provider",
      "version": "3.0.9",
      "source": "https://github.com/openssl/openssl/releases/download/openssl-3.0.9/openssl-3.0.9.tar.gz",
      "source_kind": "self-built",
      "path": "/usr/lib/ossl-modules/fips.so",
      "license": "Apache-2.0",
      "purl": "pkg:generic/openssl-fips-provider@3.0.9?download_url=https%3A%2F%2Fgithub.com%2Fopenssl%2Fopenssl%2Freleases%2Fdownload%2Fopenssl-3.0.9%2Fopenssl-3.0.9.tar.gz",
      "cpe": "cpe:2.3:a:openssl:openssl:3.0.9:*:*:*:*:*:*:*"
    },
    {
      "_description": "uv package manager binary, copied by digest-pinned COPY --from from the upstream astral-sh/uv image (Dockerfile). Its ~1012 embedded cargo-auditable crate entries are EXCLUDED from scans (req-cicd-sbom-2 - the tool's closure, not the artifact's); the executable itself is declared here. Version must match the Dockerfile COPY pin (reconciled by the req-cicd-sbom-12 guard when it lands).",
      "name": "uv",
      "version": "0.12.3",
      "source": "ghcr.io/astral-sh/uv:0.12.3@sha256:2d890623d310b57771ce840f0da5eed5fc6d657da05ffaa45d82797b53fa3abc",
      "source_kind": "copied-image",
      "path": "/bin/uv",
      "license": "MIT OR Apache-2.0",
      "purl": "pkg:github/astral-sh/uv@0.12.3"
    },
    {
      "_description": "uvx (uv tool-runner companion binary), same upstream image and rationale as uv.",
      "name": "uvx",
      "version": "0.12.3",
      "source": "ghcr.io/astral-sh/uv:0.12.3@sha256:2d890623d310b57771ce840f0da5eed5fc6d657da05ffaa45d82797b53fa3abc",
      "source_kind": "copied-image",
      "path": "/bin/uvx",
      "license": "MIT OR Apache-2.0",
      "purl": "pkg:github/astral-sh/uv@0.12.3"
    }
  ]
}
